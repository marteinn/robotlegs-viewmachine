package se.birth.robotlegs.viewmachine_signals.controls.signals
{
	import org.osflash.signals.Signal;
	
	public class RunViewMachineSignal extends Signal
	{
		public function RunViewMachineSignal(...valueClasses)
		{
			super(valueClasses);
		}
	}
}